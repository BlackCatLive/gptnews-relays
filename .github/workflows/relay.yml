name: relay

on:

  schedule:

    - cron: "*/20 * * * *"

  workflow_dispatch:

jobs:

  ping:

    runs-on: ubuntu-latest

    steps:

      - run: curl -fsS "${{ secrets.RELAY_URL }}/run?key=${{ secrets.RELAY_SECRET }}"
